<h1><%= @artist.name %></h1>
<h4>Name: <%= @artist.name %></h4>
<h4>Age: <%= @artist.age %></h4>
<h4>Genre: <%= @artist.genre %></h4>
<br />
<h2>Songs</h2>
<% @artist each do |artist| %>
  <%= render 'songs/song', { song: song } %>
<% end %>
<br />
<%= link_to "Add Song", new_artist_song_path(@artist) %>
<br />
<br />
<%= link_to "Edit Artist", edit_artist_path(:id)  %>
<%= link_to "Delete Artist", artist_path(@artist) , method: :delete %>
<%= link_to "Back to Artists", artists_path(@bboard) %>